"""Pipelines de orquestacion (reservado).

Espacio para pipelines compuestos reutilizables (preprocesamiento -> entrenamiento
-> prediccion) como objetos de primera clase. Hoy la orquestacion vive en el
``Makefile`` y en los scripts de ``scripts/``; este subpaquete queda reservado para
encapsular esos flujos de forma programatica.
"""
